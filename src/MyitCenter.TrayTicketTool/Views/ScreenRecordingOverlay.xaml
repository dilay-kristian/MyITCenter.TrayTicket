<Window x:Class="MyitCenter.TrayTicketTool.Views.ScreenRecordingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aufnahme"
        Width="260" Height="60"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border Background="#DDE74C3C" CornerRadius="8" Padding="12,8" Margin="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="6" Opacity="0.3" ShadowDepth="2" />
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Ellipse Grid.Column="0" Width="12" Height="12" Fill="White"
                     x:Name="RecordingDot" Margin="0,0,10,0" VerticalAlignment="Center" />

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TimerText" Text="REC 00:00"
                           FontSize="15" FontWeight="Bold" Foreground="White" />
            </StackPanel>

            <Button Grid.Column="2" Content="Stop"
                    Padding="10,4" Foreground="White" FontWeight="Bold" FontSize="12"
                    Background="#AA000000" BorderThickness="0" Cursor="Hand"
                    Click="Stop_Click" VerticalAlignment="Center" />
        </Grid>
    </Border>
</Window>
